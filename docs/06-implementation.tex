\chapter{Технологический раздел}

\section{Обоснование выбора программных средств}
Для реализации предложенного метода выбран язык Python, поскольку он имеет развитую экосистему библиотек для машинного обучения, обработки изображений и построения прикладных интерфейсов. Основная вычислительная часть реализуется на базе PyTorch. Данная библиотека предоставляет тензорные операции, автоматическое дифференцирование, поддержку GPU и удобный механизм сохранения параметров модели, что необходимо для обучения сиамских нейронных сетей и последующего использования обученных весов.

В качестве источника предобученных базовых свёрточных сетей используется библиотека TorchVision. Это позволяет применять архитектуры свёрточных сетей, такие как VGG16, EfficientNet и MobileNet, не реализуя их вручную. Использование предобученных моделей снижает требования к объёму размеченных данных и ускоряет сходимость при обучении на изображениях растений.

Для работы с изображениями используется Pillow. Библиотека применяется для загрузки изображений, преобразования их в формат RGB и передачи в цепочку преобразований перед подачей в модель. Конфигурация экспериментов хранится в YAML-файлах, поэтому для чтения настроек используется PyYAML. Такой формат удобен для фиксации параметров набора данных, архитектуры модели, обучения, построения пар и инференса без изменения исходного кода.

Для пользовательского приложения выбран PyQt6, так как он позволяет реализовать интерфейс для выбора изображений, загрузки артефактов модели и отображения результатов классификации без развёртывания серверной части. Интерфейс разделён на несколько сценариев: выбор входных изображений или каталога, загрузка моделей, запуск распознавания, просмотр результатов, проверка корректности классификации и очистка рабочей области.

\section{Формат входных данных}
Входные данные метода состоят из изображений растений, метаданных набора данных и конфигурационного файла. Изображения хранятся в файловой системе, а в таблице метаданных для каждого изображения указываются путь к файлу и таксономические признаки растения. Поля таблицы метаданных приведены в таблице~\ref{table:metadata-fields}.

\begin{table}[!htbp]
	\centering
	\caption{Поля таблицы метаданных}
	\label{table:metadata-fields}
	\footnotesize
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\linewidth}{|p{4.2cm}|X|}
		\hline
		\textbf{Поле} & \textbf{Описание} \\
		\hline
		\texttt{image\_path} & Путь к файлу изображения. Если путь относительный, он интерпретируется относительно корневого каталога набора данных, указанного в конфигурации. \\
		\hline
		\texttt{family} & Название семейства растения, используемое как дополнительный таксономический признак и контекст для группировки родов. \\
		\hline
		\texttt{genus} & Название рода растения. Поле используется как целевой класс при обучении модели S-CNN-1. \\
		\hline
		\texttt{species} & Название вида растения. Поле используется как целевой класс при обучении модели S-CNN-2 и при формировании итогового результата классификации. \\
		\hline
		\texttt{split} & Часть выборки, к которой относится изображение: \texttt{train}, \texttt{val} или \texttt{test}. Поле необязательно; если оно отсутствует, записи используются без фильтрации по разбиению. \\
		\hline
	\end{tabularx}
\end{table}
\FloatBarrier

Основные значения YAML-конфигурации, выбранные для эксперимента, приведены в таблице~\ref{table:yaml-config}.

\begin{table}[!htbp]
	\centering
	\caption{Параметры YAML-конфигурации}
	\label{table:yaml-config}
	\footnotesize
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\linewidth}{|p{4.5cm}|X|p{4.6cm}|}
		\hline
		\textbf{Название} & \textbf{Описание} & \textbf{Выбранное значение} \\
		\hline
		\texttt{dataset.name} & Название набора данных в рамках эксперимента. & \texttt{PlantCLEF2015}\newline\texttt{LeafScanPaper60} \\
		\hline
		\texttt{dataset.root} & Корневой каталог изображений. & \texttt{data/plantclef2015/}\newline\texttt{leafscan} \\
		\hline
		\texttt{dataset.metadata} & Файл с таблицей метаданных. & \texttt{data/plantclef2015/}\newline\texttt{leafscan\_paper60\_}\newline\texttt{metadata.csv} \\
		\hline
		\texttt{dataset.subset} & Ограничение числа изображений одного вида при формировании подвыборки. & \texttt{min=6; max=6; seed=42} \\
		\hline
		\texttt{views.global.}\newline\texttt{image\_size} & Размер изображения для глобального представления. & \texttt{224} \\
		\hline
		\texttt{views.local.}\newline\texttt{image\_size} & Размер изображения для локального представления. & \texttt{224} \\
		\hline
		\texttt{views.local.}\newline\texttt{crop\_size} & Размер центрального фрагмента локального представления. & \texttt{32} \\
		\hline
		\texttt{preprocessing.}\newline\texttt{enabled} & Включение предобработки изображений. & \texttt{true} \\
		\hline
		\texttt{preprocessing.}\newline\texttt{leaf\_bbox} & Включение выделения области листа. & \texttt{true} \\
		\hline
		\texttt{pair\_sampling.}\newline\texttt{positive\_per\_epoch} & Число положительных пар на эпоху для уровней рода и вида. & \texttt{genus=400;}\newline\texttt{species=800} \\
		\hline
		\texttt{pair\_sampling.}\newline\texttt{negative\_per\_epoch} & Число отрицательных пар на эпоху для уровней рода и вида. & \texttt{genus=600;}\newline\texttt{species=1200} \\
		\hline
		\texttt{pair\_sampling.}\newline\texttt{hard\_negative\_ratio} & Доля сложных отрицательных пар в выбранной конфигурации. & \texttt{0.0} \\
		\hline
		\texttt{model.backbone} & Базовая свёрточная сеть. & \texttt{vgg16} \\
		\hline
		\texttt{model.pretrained} & Использование предобученных весов. & \texttt{true} \\
		\hline
		\texttt{training.}\newline\texttt{batch\_size} & Размер batch при обучении. & \texttt{32} \\
		\hline
		\texttt{training.epochs} & Число эпох обучения. & \texttt{80} \\
		\hline
		\texttt{training.}\newline\texttt{optimizer} & Алгоритм оптимизации. & \texttt{sgd} \\
		\hline
		\texttt{training.}\newline\texttt{learning\_rate} & Скорость обучения. & \texttt{0.001} \\
		\hline
		\texttt{training.momentum} & Коэффициент момента оптимизатора. & \texttt{0.9} \\
		\hline
		\texttt{training.}\newline\texttt{max\_iterations} & Ограничение числа итераций. & \texttt{2048} \\
		\hline
		\texttt{training.}\newline\texttt{lr\_decay\_step} & Шаг уменьшения скорости обучения. & \texttt{512} \\
		\hline
		\texttt{training.}\newline\texttt{lr\_decay\_gamma} & Множитель уменьшения скорости обучения. & \texttt{0.5} \\
		\hline
		\texttt{inference.}\newline\texttt{references\_per\_class} & Число эталонов на класс. & \texttt{6} \\
		\hline
		\texttt{inference.}\newline\texttt{genus\_candidates} & Число родов-кандидатов. & \texttt{30} \\
		\hline
		\texttt{inference.top\_k} & Размер итогового ранжированного списка. & \texttt{5} \\
		\hline
	\end{tabularx}
\end{table}
\FloatBarrier

Внутри программы запись об изображении представляется сущностью \texttt{ImageRecord}, структура которой приведена в таблице~\ref{table:image-record-format}. При обучении из таких записей формируются сущности \texttt{PairRecord}, их структура приведена в таблице~\ref{table:pair-record-format}.

\begin{table}[!htbp]
	\centering
	\caption{Структура сущности \texttt{ImageRecord}}
	\label{table:image-record-format}
	\footnotesize
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\linewidth}{|p{3.4cm}|X|p{2.8cm}|p{3.6cm}|}
		\hline
		\textbf{Поле} & \textbf{Описание} & \textbf{Тип} & \textbf{Ограничение} \\
		\hline
		\texttt{image\_path} & Путь к изображению растения. & \texttt{Path} & Файл должен существовать на этапе загрузки изображения. \\
		\hline
		\texttt{family} & Семейство растения. & строка & Значение не должно быть пустым для корректного таксономического описания. \\
		\hline
		\texttt{genus} & Род растения. & строка & Используется как метка уровня \texttt{genus}. \\
		\hline
		\texttt{species} & Вид растения. & строка & Используется как метка уровня \texttt{species}. \\
		\hline
		\texttt{split} & Часть выборки. & строка & Допустимы значения \texttt{train}, \texttt{val}, \texttt{test} или пустая строка. \\
		\hline
	\end{tabularx}
\end{table}
\FloatBarrier

\begin{table}[!htbp]
	\centering
	\caption{Структура сущности \texttt{PairRecord}}
	\label{table:pair-record-format}
	\footnotesize
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\linewidth}{|p{3.4cm}|X|p{2.8cm}|p{3.6cm}|}
		\hline
		\textbf{Поле} & \textbf{Описание} & \textbf{Тип} & \textbf{Ограничение} \\
		\hline
		\texttt{left} & Путь к первому изображению пары. & \texttt{Path} & Должен указывать на изображение из обучающей подвыборки. \\
		\hline
		\texttt{right} & Путь ко второму изображению пары. & \texttt{Path} & Должен указывать на изображение из обучающей подвыборки. \\
		\hline
		\texttt{label} & Бинарная метка сходства пары. & целое число & Значение \texttt{1} соответствует одному классу, значение \texttt{0}~---~разным классам. \\
		\hline
		\texttt{taxonomic\_level} & Таксономический уровень сравнения. & строка & Для предложенного метода используются значения \texttt{genus} и \texttt{species}. \\
		\hline
	\end{tabularx}
\end{table}
\FloatBarrier

\section{Формат выходных данных}
В результате обучения формируются файлы параметров моделей S-CNN-1 и S-CNN-2, сохранённые средствами PyTorch. После обучения строится эталонный индекс, содержащий заранее рассчитанные эмбеддинги эталонных изображений. При классификации отдельного изображения метод возвращает ранжированный список гипотез о виде растения. Поля записи эталонного индекса приведены в таблице~\ref{table:reference-embedding-fields}, а поля результата классификации~---~в таблице~\ref{table:prediction-fields}.

\begin{table}[!htbp]
	\centering
	\caption{Поля записи эталонного индекса}
	\label{table:reference-embedding-fields}
	\footnotesize
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\linewidth}{|p{3.8cm}|X|p{2.6cm}|p{3.6cm}|}
		\hline
		\textbf{Поле} & \textbf{Описание} & \textbf{Тип} & \textbf{Ограничение} \\
		\hline
		\texttt{image\_path} & Путь к эталонному изображению. & \texttt{Path} & Используется для связи эмбеддинга с исходным изображением. \\
		\hline
		\texttt{family} & Семейство эталонного растения. & строка & Берётся из метаданных. \\
		\hline
		\texttt{genus} & Род эталонного растения. & строка & Используется для отбора родов-кандидатов. \\
		\hline
		\texttt{species} & Вид эталонного растения. & строка & Используется для группировки итоговых гипотез. \\
		\hline
		\texttt{global\_embedding} & Глобальное признаковое представление эталона. & \texttt{Tensor} & Размерность определяется выбранной базовой сетью. \\
		\hline
		\texttt{local\_embedding} & Локальное признаковое представление эталона. & \texttt{Tensor} & Размерность определяется выбранной базовой сетью. \\
		\hline
	\end{tabularx}
\end{table}
\FloatBarrier

\begin{table}[!htbp]
	\centering
	\caption{Поля результата классификации}
	\label{table:prediction-fields}
	\footnotesize
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\linewidth}{|p{3.8cm}|X|p{2.6cm}|p{3.6cm}|}
		\hline
		\textbf{Поле} & \textbf{Описание} & \textbf{Тип} & \textbf{Ограничение} \\
		\hline
		\texttt{PredictionLabel.}\newline\texttt{family} & Семейство найденной гипотезы. & строка & Передаётся в пользовательский интерфейс вместе с видом. \\
		\hline
		\texttt{PredictionLabel.}\newline\texttt{genus} & Род найденной гипотезы. & строка & Используется при отображении биномиального названия. \\
		\hline
		\texttt{PredictionLabel.}\newline\texttt{species} & Вид найденной гипотезы. & строка & Основной класс, возвращаемый пользователю. \\
		\hline
		\texttt{PredictionLabel.}\newline\texttt{score} & Оценка сходства входного изображения с эталонами данного вида. & число с плавающей точкой & Значение находится в диапазоне от \texttt{0} до \texttt{1}. \\
		\hline
		\texttt{ImagePrediction.}\newline\texttt{image\_path} & Путь к классифицируемому изображению. & \texttt{Path} & Должен соответствовать входному файлу пользователя. \\
		\hline
		\texttt{ImagePrediction.}\newline\texttt{labels} & Ранжированный список найденных гипотез. & кортеж меток & Длина ограничивается параметром \texttt{top\_k}. \\
		\hline
		\texttt{ImagePrediction.}\newline\texttt{error} & Сообщение об ошибке обработки изображения. & строка или \texttt{None} & Заполняется только при неуспешной обработке. \\
		\hline
	\end{tabularx}
\end{table}
\FloatBarrier

Итоговый результат инференса представляет собой ранжированный список наиболее вероятных видов. Для каждого вида указывается оценка сходства с эталонными изображениями: чем выше эта оценка, тем более вероятным считается соответствующий вид растения.

\section{Взаимодействие пользователя с программным обеспечением}
В пользовательском приложении предусмотрены команды выбора отдельных изображений или каталога, загрузки артефактов модели, запуска распознавания, проверки корректности классификации и очистки рабочей области. При необходимости список входных файлов может отображаться только по именам. Перед распознаванием пользователь загружает контрольную точку S-CNN-1, контрольную точку S-CNN-2 и эталонный индекс. После запуска распознавания приложение показывает результаты классификации в виде сетки карточек, как показано на рисунке~\ref{fig:app-grid-predictions}.

\begin{figure}[!htbp]
	\centering
	\includegraphics[width=0.95\linewidth,height=0.46\textheight,keepaspectratio]{inc/img/demo/app_grid_predictions.png}
	\caption{Представление результатов классификации в виде сетки}
	\label{fig:app-grid-predictions}
\end{figure}
\FloatBarrier

Для детального просмотра пользователь может перейти на вкладку отдельного изображения. На этой вкладке отображается увеличенное изображение и список наиболее вероятных видов с указанием семейства и оценки сходства. Пример такого просмотра приведён на рисунке~\ref{fig:app-image-prediction}.

\begin{figure}[!htbp]
	\centering
	\includegraphics[width=0.95\linewidth,height=0.46\textheight,keepaspectratio]{inc/img/demo/app_image_prediction.png}
	\caption{Просмотр наиболее вероятных видов для отдельного изображения}
	\label{fig:app-image-prediction}
\end{figure}
\FloatBarrier

Опция проверки корректности классификации используется, когда рядом с изображением доступна разметка или файл метаданных с правильным видом. При включении этой опции приложение сравнивает верхнюю гипотезу с правильным видом, выделяет результат цветом и выводит информацию о проверке. Результат классификации для отдельного изображения показан на рисунке~\ref{fig:app-image-check}, а представление нескольких результатов классификации в сетке~---~на рисунке~\ref{fig:app-grid-check}.

\begin{figure}[!htbp]
	\centering
	\includegraphics[width=0.95\linewidth,height=0.46\textheight,keepaspectratio]{inc/img/demo/app_image_check.png}
	\caption{Результат классификации для отдельного изображения}
	\label{fig:app-image-check}
\end{figure}
\FloatBarrier

\begin{figure}[!htbp]
	\centering
	\includegraphics[width=0.95\linewidth,height=0.46\textheight,keepaspectratio]{inc/img/demo/app_grid_check.png}
	\caption{Отображение результатов классификации в сетке}
	\label{fig:app-grid-check}
\end{figure}
\FloatBarrier

После завершения работы пользователь может очистить список изображений, результаты распознавания и состояние проверки корректности классификации. Это возвращает приложение к начальному состоянию и позволяет выполнить новый сценарий распознавания без перезапуска программы.

\section{Вывод}

В данном разделе был обоснован выбор программных средств для реализации разработанного метода. Были описаны форматы входных данных, структура конфигурации, форматы выходных данных и содержимое эталонного индекса. Также было рассмотрено взаимодействие пользователя с программным обеспечением при выборе изображений, запуске распознавания, просмотре результатов и проверке корректности классификации.
